import Foundation

final class IPCClient {
    func connect() async throws {
        // TODO: connect to guest IPC endpoint.
    }

    func send(_ message: BridgeMessage) async throws {
        // TODO: send message to guest.
        _ = message
    }
}
